package group1.tut.entities.services;

public class LoginService {

    // private final LoginRepository loginRepository;
    // private final AccountRepository accountRepository;

    // public LoginService(LoginRepository loginRepository, AccountRepository accountRepository) {
    //     this.loginRepository = loginRepository;
    //     this.accountRepository = accountRepository;
    // }

    // public Login authenticate(LoginRequest loginRequest) {
    //     String username = loginRequest.getUsername();
    //     String password = loginRequest.getPassword();

    //     Optional<Account> accountOptional = accountRepository.findByUsername(username);
    //     if (accountOptional.isPresent()) {
    //         Account account = accountOptional.get();
    //         if (account.getPassword().equals(password)) {
    //             return new Login(account.getId(), username);
    //         }
    //     }
    //     return null;
    // }
    
}
